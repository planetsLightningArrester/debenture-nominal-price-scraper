name: 🏊 Scrap data
concurrency:
  cancel-in-progress: false
  group: scrap-data

on:
  workflow_dispatch:
  schedule:
    - cron: '* * * * 1,2,3,4,5'
jobs:
  daily_check:
    name: "🏊 Scrap data"
    runs-on: self-hosted
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🛠️ NodeJS setup
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 🔧 Install dependencies
        run: npm ci

      - name: 🔄 Run scraper
        run: |
          npm run start -- -g '${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON_KEY }}'

      - uses: actions/upload-artifact@v4
        if: ${{ failure() }}
        with:
          name: Errors
          path: ./*.png